<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.aistudio.system.mapper.IKnowledgeBaseMapper">

    <resultMap id="KnowledgeBaseResultMap" type="com.aistudio.system.entity.KnowledgeBaseDO">
        <id column="id" property="id"/>
        <result column="name" property="name"/>
        <result column="description" property="description"/>
        <result column="status" property="status"/>
        <result column="published_status" property="publishedStatus"/>
        <result column="document_count" property="documentCount"/>
        <result column="chunk_count" property="chunkCount"/>
        <result column="create_time" property="createTime"/>
        <result column="update_time" property="updateTime"/>
        <result column="create_by" property="createBy"/>
        <result column="update_by" property="updateBy"/>
        <result column="deleted" property="deleted"/>
    </resultMap>

    <sql id="KnowledgeBaseColumns">
        id,
        name,
        description,
        status,
        published_status,
        document_count,
        chunk_count,
        create_time,
        update_time,
        create_by,
        update_by,
        deleted
    </sql>

    <select id="selectKnowledgeBaseById" resultMap="KnowledgeBaseResultMap">
        select
        <include refid="KnowledgeBaseColumns"/>
        from knowledge_base
        where id = #{id}
          and deleted = 0
    </select>

    <select id="selectKnowledgeBasePage" resultMap="KnowledgeBaseResultMap">
        select
        <include refid="KnowledgeBaseColumns"/>
        from knowledge_base
        where deleted = 0
        <if test="query.name != null and query.name != ''">
            and name like concat('%', #{query.name}, '%')
        </if>
        <if test="query.status != null">
            and status = #{query.status}
        </if>
        <if test="query.createStartTime != null">
            and create_time <![CDATA[>=]]> #{query.createStartTime}
        </if>
        <if test="query.createEndTime != null">
            and create_time <![CDATA[<=]]> #{query.createEndTime}
        </if>
        order by update_time desc, id desc
    </select>

    <select id="countByName" resultType="java.lang.Integer">
        select count(1)
        from knowledge_base
        where deleted = 0
          and name = #{name}
        <if test="excludeId != null">
            and id != #{excludeId}
        </if>
    </select>

    <update id="softDeleteById">
        update knowledge_base
        set deleted = 1
        where id = #{id}
          and deleted = 0
    </update>
</mapper>
