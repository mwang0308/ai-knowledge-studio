# 召回测试

## 目标

在发布前验证知识分片能否被正确召回，降低发布后问答不可命中的风险。

## 核心能力

- 输入测试问题。
- 选择测试范围：文档、目录、知识库。
- 设置 TopK。
- 展示召回分片、相似度、来源、页码。
- 保存测试历史。

## 质量判断

- 目标文档是否命中。
- 目标分片是否进入 TopK。
- 命中摘要是否能回答问题。
- 低质量召回需要标记原因。

## 约束

- 召回测试入口在 `system-service`。
- 具体检索编排由 `ai-agent-service` 执行。
- 测试结果不能直接影响发布状态，发布仍需审核确认。
