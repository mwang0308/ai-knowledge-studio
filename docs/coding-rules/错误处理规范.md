# 错误处理规范

## 错误分类

- 参数错误。
- 业务规则错误。
- 外部服务错误。
- 系统异常。

## Java 处理

- 参数错误通过校验注解和全局异常处理返回。
- 业务错误抛出业务异常。
- 外部服务错误必须记录上下文日志。
- 异步任务失败必须记录失败原因和重试次数。

## Python 处理

- 解析失败必须返回错误码和错误消息。
- 回调 Java 时必须明确 `FAILED` 状态。
- 失败任务允许重试，但必须保留历史记录。

## 前端展示

- 用户能看懂的错误要直接展示。
- 技术细节只记录日志，不直接暴露给普通用户。
